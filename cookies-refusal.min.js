const a = document.querySelectorAll('input[type="checkbox"][class*="consent"]'); a.forEach(b => b.checked = false); console[a.length ? 'log' : 'warn'](a.length ? `${a.length} cookie consent checkboxes have been unchecked.` : 'No cookie consent checkboxes found. Please ensure the cookie panel is visible.');
